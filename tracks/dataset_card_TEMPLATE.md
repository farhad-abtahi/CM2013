# Dataset card — <TRACK NAME>

*Auto-render the metadata table with `TrackAdapter.dataset_card()`, then add the
human "Known pitfalls" below. Every field maps to a `TrackMeta` attribute.*

| Field | Value |
|---|---|
| Dataset | `<dataset>` (v`<DATASET_VERSION>`) |
| License / access | `<LICENSE>` · **open-direct (no agreement)** |
| Signals | `<signals>` |
| Task | `<TASK_TYPE>` — classes: `<classes>` |
| **Split unit (leakage)** | **`<SPLIT_UNIT>`** |
| Evaluation modes | `<new-subject | within-subject>` |
| Default metrics | `<DEFAULT_METRICS>` |
| Smoke-test records | `<SMOKE_TEST_RECORDS>` |
| Expected runtime | `<EXPECTED_RUNTIME>` |
| DSP focus | `<DSP_FOCUS>` |
| Difficulty (1–5) | `<difficulty>` |

**Citation.** `<CITATION>`  
**Source.** `<url>`

## Known pitfalls
- <label merges / imbalance / artifacts / window-overlap leakage / montage choices …>

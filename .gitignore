__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store
.pytest_cache/

# CI-generated (built by .github/workflows/deploy.yml, published to Pages — not committed)
# docs/index.html and docs/notebooks.json ARE committed (the hand-authored gallery page).
docs/nb/
docs/lite/
_lite_content/
.jupyterlite.doit.db

# local dataset cache for the capstone tracks — students download real data
# themselves (see each track's dataset card); never committed here
data_cache/

# instructor-only lab solutions must never land in this public student repo —
# the real source of truth lives outside this repo entirely (companion-course/);
# this entry is a defensive backstop in case a solutions/ dir is ever added here
labs/solutions/
**/solutions/

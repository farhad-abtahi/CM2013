__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store
.pytest_cache/

# CI-generated (built by .github/workflows/deploy.yml, published to Pages — not committed)
# docs/index.html and docs/notebooks.json ARE committed (the hand-authored gallery page).
docs/nb/
docs/lite/
_lite_content/
.jupyterlite.doit.db

# local dataset cache for the capstone tracks — students download real data
# themselves (see each track's dataset card); never committed here
data_cache/
